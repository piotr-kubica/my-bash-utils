#!/usr/bin/env bash
echo "<span color='#E85' font-family='Monospace' weight='normal'><small>Emacs...</small></span> | length=40"
echo "---"
echo "emacs | bash='emacs -q -l ~/.emacs.d-std/init.el'"
echo "org | bash='emacs -q -l ~/.emacs.d-org/init.el'"
echo "py | bash='source ~/pyenv/bin/activate; emacs -q -l ~/.emacs.d-py/init.el'"
echo "elm | bash='emacs -q -l ~/.emacs.d-elm/init.el'"
echo "lisp-clj | bash='emacs -q -l ~/.emacs.d-lisp-clj/init.el'"
